<p style="color: green"><%= notice %></p>

<% content_for :title, "Todos" %>

<h1>Todos</h1>

<div id="todos">
  <% @todos.each do |todo| %>
    <%= render "todos/todo_row", todo: todo %>
  <% end %>
</div>

<%= link_to "New todo", new_todo_path %>
